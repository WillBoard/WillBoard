@using Microsoft.AspNetCore.Routing
@using WillBoard.Application.Administration.Commands.BoardPostDeleteFile
@using WillBoard.Application.Administration.Queries.BoardPostDeleteFile
@using WillBoard.Core.Managers
@using WillBoard.Web.Services
@using WillBoard.Core.Extensions
@inject LinkGenerator _linkGenerator
@inject ViewService _viewService
@inject AccountManager _accountManager
@model BoardPostDeleteFileViewModel
@{
    Layout = "~/Views/Shared/_AdministrationLayout.cshtml";
}

<main>
    <article>
        <header>Board - Post - Delete File</header>
        <section>

            <form method="post">
                <input type="hidden" name="csrf" value="@ViewData["csrf"]" autocomplete="off">
                <input type="hidden" name="@nameof(BoardPostDeleteFileCommand.BoardId)" value="@Model.Post.BoardId" autocomplete="off">
                <input type="hidden" name="@nameof(BoardPostDeleteFileCommand.PostId)" value="@Model.Post.PostId" autocomplete="off">
                <p>
                    <button type="submit">Delete @Model.Post.BoardId/@Model.Post.PostId file</button>
                </p>
            </form>

        </section>
    </article>
</main>